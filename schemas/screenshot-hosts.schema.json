{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OmniSource confirmed screenshot hosts",
  "description": "Developer-owned hosts that serve app screenshots instead of the app's upstream host. Entries are reviewed decisions (reason + date + references); validate.py stops warning about an allowlisted host. Structural enforcement lives in src/omnisource/validation.py; this document is the human-readable contract the validator mirrors.",
  "type": "object",
  "required": ["version", "hosts"],
  "properties": {
    "version": { "const": 1 },
    "$schema": { "type": "string" },
    "description": { "type": "string" },
    "hosts": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["reason", "decidedAt"],
        "properties": {
          "reason": { "type": "string", "minLength": 10 },
          "decidedAt": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
          "references": { "type": "array", "items": { "type": "string", "pattern": "^https://" } }
        }
      }
    }
  }
}
